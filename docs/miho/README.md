# MiHO Partners — reference docs

Shared context for MiHO (Mike + Howard's SMB tools/content venture), kept here for visibility — not installable plugins.

## brand/
Ground-truth brand knowledge: positioning, voice rules, ICP, and proven content angles. Portable — read these before writing any MiHO copy, LinkedIn post, or ad.

## commands/
`staff-writer.md` and `social-package.md` are Mike's local Claude Code slash commands for the MiHO content pipeline (topic discovery → article → WordPress/LinkedIn, and article → full platform content package).

**These are reference material, not runnable as-is.** They're hardcoded to Mike's machine:
- Local folder tree under `C:\Users\mike\viral-topics\...`
- Mike's WordPress site (michaelgrabham.com) and Gmail app password
- Mike's HeyGen avatar config and WoopSocial project

To actually run either command, you'd need to rebuild that local folder structure and credentials yourself — this copy is here so Howard can see how the pipeline works, not to install and use directly.
